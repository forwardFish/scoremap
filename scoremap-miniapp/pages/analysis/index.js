if (typeof Page === 'function') {  const { createReplicaPage } = require('../../utils/replica-runtime');  createReplicaPage({  "title": "AI 正在分析",  "reference": "/assets/reference/analysis.jpg",  "derived": false,  "hotspots": [    {      "action": "later",      "label": "稍后查看",      "className": "analysis-later"    },    {      "action": "refresh",      "label": "刷新进度",      "className": "analysis-refresh"    }  ],  "actions": {    "later": {      "url": "/pages/reports/index"    },    "refresh": {      "url": "/pages/preview/index"    }  },  "cards": []});} else {const { createMiniappApiClient } = require('../../services/api-client');const ANALYSIS_ROUTE = '/pages/analysis/index';const FAILURE_ROUTE = '/pages/failure/index';const MY_REPORTS_ROUTE = '/pages/reports/index';const PREVIEW_ROUTE = '/pages/preview/index';const DEFAULT_STEPS = [  { id: 'read-material', text: '已识别上传资料', status: 'done' },  { id: 'match-subject', text: '已匹配年级与学科', status: 'done' },  { id: 'locate-loss-points', text: '正在定位主要丢分点', status: 'active' },  { id: 'generate-preview', text: '正在生成初步建议', status: 'pending' }];function createAnalysisPageState(client = createMiniappApiClient(), options = {}) {  const orderId = options.orderId || 'order-t08-analysis';  const pollIntervalMs = options.pollIntervalMs || 2000;  const timeoutMs = options.timeoutMs || 30000;  let lastProgress = {    status: 'analyzing',    progress: 68,    currentStep: 'locate-loss-points',    steps: DEFAULT_STEPS  };  let toast = null;  const page = {    route: ANALYSIS_ROUTE,    orderId,    uiReference: {      png: 'ui-reference-catalog/小程序/AI分析.png',      stitch: 'ui-reference-catalog/小程序/stitch_codex_development_blueprints/ai_1/screen-reference'    },    title: 'AI 正在分析中',    estimateText: '通常需要 10-30 秒',    pollIntervalMs,    timeoutMs,    controls: [      { id: 'later-view', text: '稍后查看', run: () => page.laterView() },      { id: 'refresh-progress', text: '刷新进度', run: () => page.refreshProgress() }    ],    getState() {      return {        route: ANALYSIS_ROUTE,        orderId,        title: page.title,        estimateText: page.estimateText,        progress: lastProgress.progress,        status: lastProgress.status,        currentStep: lastProgress.currentStep,        steps: lastProgress.steps,        pollIntervalMs,        timeoutMs,        toast,        controls: page.controls.map((control) => ({ id: control.id, text: control.text })),        uiReference: page.uiReference      };    },    pollProgress(elapsedMs = 0) {      return page.loadProgress({ source: 'poll', elapsedMs });    },    refreshProgress() {      return page.loadProgress({ source: 'manual-refresh', elapsedMs: 0 });    },    loadProgress({ source, elapsedMs }) {      const response = client.request('GET', `/api/diagnosis-orders/${orderId}/analysis-progress`, { source, elapsedMs });      lastProgress = normalizeProgress(response.body);      toast = null;      if (lastProgress.status === 'preview_done' || lastProgress.progress >= 100) {        return {          status: 'READY',          targetRoute: PREVIEW_ROUTE,          source,          progress: lastProgress.progress        };      }      if (lastProgress.status === 'preview_failed' || lastProgress.status === 'failed') {        return {          status: 'FAILED',          targetRoute: FAILURE_ROUTE,          source,          progress: lastProgress.progress,          errorCode: lastProgress.errorCode || 'ai_failed'        };      }      if (elapsedMs >= timeoutMs) {        return {          status: 'TIMEOUT',          targetRoute: FAILURE_ROUTE,          source,          elapsedMs,          errorCode: 'analysis_timeout'        };      }      return {        status: 'PENDING',        targetRoute: ANALYSIS_ROUTE,        source,        nextPollInMs: pollIntervalMs,        progress: lastProgress.progress      };    },    laterView() {      const response = client.request('GET', '/api/my/reports', { source: 'analysis-later-view', orderId });      toast = '已保存到我的报告，可稍后继续查看';      return {        status: 'NAVIGATE',        targetRoute: MY_REPORTS_ROUTE,        reportsCount: response.body.items.length,        toast      };    }  };  return page;}function normalizeProgress(body) {  return {    status: body.status || 'analyzing',    progress: Number.isFinite(body.progress) ? body.progress : 0,    currentStep: body.currentStep || 'locate-loss-points',    steps: Array.isArray(body.steps) && body.steps.length > 0 ? body.steps : DEFAULT_STEPS,    errorCode: body.errorCode || null  };}module.exports = { createAnalysisPageState };}