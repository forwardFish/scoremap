const assert = require('node:assert/strict');const fs = require('node:fs');const path = require('node:path');const { test } = require('node:test');const { writeJsonEvidence } = require('../../../shared/evidence-paths');const { DEFAULT_FORBIDDEN_PATTERNS, assertLocalOnlyEnvironment, scanTextForForbiddenRemoteCalls } = require('../../../shared/local-only');const { createMiniappApiClient } = require('../../services/api-client');const { createAnalysisPageState } = require('./index');const { createFailurePageState } = require('../failure');const projectRoot = path.resolve(__dirname, '..', '..', '..');const command = 'npm test -- analysis-failure';function writeEvidence(name, payload) {  writeJsonEvidence(projectRoot, path.join('frontend-page', name), payload);}function seedAnalysisTask(client, overrides = {}) {  const orderId = overrides.orderId || 'order-t08-analysis';  client.store.upsert('diagnosis_orders', {    id: orderId,    ownerId: 'local-user-scoremap-t06',    status: overrides.status || 'analyzing',    accessLevel: 'preview',    source: 'T08-analysis-failure'  });  client.store.upsert('ai_analysis_tasks', {    id: overrides.taskId || 'task-t08-analysis',    orderId,    ownerId: 'local-user-scoremap-t06',    type: 'preview',    status: overrides.status || 'analyzing',    progress: overrides.progress ?? 68,    currentStep: overrides.currentStep || 'locate-loss-points',    errorCode: overrides.errorCode || null,    retryCount: overrides.retryCount || 0  });  return orderId;}test('analysis page renders C03 progress controls, 2 second polling, manual refresh, later view, and completion jump', () => {  const client = createMiniappApiClient();  const orderId = seedAnalysisTask(client);  const page = createAnalysisPageState(client, { orderId });  const initial = page.getState();  const pending = page.pollProgress(2000);  const refresh = page.refreshProgress();  const later = page.laterView();  client.store.upsert('ai_analysis_tasks', {    ...client.store.read('ai_analysis_tasks', 'task-t08-analysis'),    status: 'preview_done',    progress: 100,    currentStep: 'preview_ready'  });  client.store.upsert('diagnosis_decisions', {    id: `decision-${orderId}-preview`,    orderId,    ownerId: 'local-user-scoremap-t06',    level: 'preview',    preview: { visibleModules: ['overview', 'loss-points', 'advice'], lockedModules: ['full-report'] }  });  client.store.upsert('diagnosis_orders', { ...client.store.read('diagnosis_orders', orderId), status: 'preview_done' });  const complete = page.pollProgress(4000);  assert.equal(initial.route, '/pages/analysis/index');  assert.equal(initial.pollIntervalMs, 2000);  assert.equal(initial.timeoutMs, 30000);  assert.deepEqual(initial.controls.map((control) => control.id), ['later-view', 'refresh-progress']);  assert.equal(pending.status, 'PENDING');  assert.equal(pending.nextPollInMs, 2000);  assert.equal(refresh.status, 'PENDING');  assert.equal(later.targetRoute, '/pages/reports/index');  assert.equal(complete.status, 'READY');  assert.equal(complete.targetRoute, '/pages/preview/index');  writeEvidence('analysis-page-route-controls.json', {    status: 'PASS',    command,    requirementIds: ['R03'],    ownerScenarioIds: ['O02', 'O07'],    uiReference: initial.uiReference,    route: initial.route,    renderedState: page.getState(),    pageJumpEvidence: [      { controlId: 'refresh-progress', result: refresh },      { controlId: 'later-view', result: later },      { event: 'auto-complete', result: complete }    ],    apiCalls: client.calls  });});test('analysis and failure pages record local API calls, DB readbacks, timeout, failure, and retry recovery', () => {  const client = createMiniappApiClient();  const orderId = seedAnalysisTask(client, {    orderId: 'order-t08-failure',    taskId: 'task-t08-failure',    status: 'preview_failed',    progress: 0,    errorCode: 'blurry_material'  });  const analysis = createAnalysisPageState(client, { orderId });  const failureJump = analysis.refreshProgress();  const timeoutClient = createMiniappApiClient();  const timeoutOrderId = seedAnalysisTask(timeoutClient, {    orderId: 'order-t08-timeout',    taskId: 'task-t08-timeout',    status: 'analyzing',    progress: 72  });  const timeoutPage = createAnalysisPageState(timeoutClient, { orderId: timeoutOrderId });  const timeout = timeoutPage.pollProgress(30000);  const failure = createFailurePageState(client, { orderId, errorCode: 'blurry_material' });  const retry = failure.retryAnalysis();  const reupload = failure.reupload();  const home = failure.backHome();  const snapshot = client.store.snapshot();  assert.equal(failureJump.status, 'FAILED');  assert.equal(failureJump.targetRoute, '/pages/failure/index');  assert.equal(timeout.status, 'TIMEOUT');  assert.equal(timeout.targetRoute, '/pages/failure/index');  assert.equal(retry.status, 'RETRY_STARTED');  assert.equal(retry.targetRoute, '/pages/analysis/index');  assert.equal(reupload.targetRoute, '/pages/index/index');  assert.equal(home.targetRoute, '/pages/index/index');  assert.equal(client.store.read('ai_analysis_tasks', `task-t08-retry-${orderId}`).retryCount, 1);  assert.equal(client.store.read('diagnosis_orders', orderId).status, 'analyzing');  writeEvidence('analysis-failure-api-db.json', {    status: 'PASS',    command,    requirementIds: ['R03', 'R04'],    ownerScenarioIds: ['O08', 'O09', 'O11'],    apiEvidence: {      failureProgressCall: client.calls.find((call) => call.path.endsWith('/analysis-progress')),      retryCall: client.calls.find((call) => call.path.endsWith('/start-preview-analysis')),      timeoutCall: timeoutClient.calls.find((call) => call.path.endsWith('/analysis-progress'))    },    pageJumpEvidence: [      { event: 'failed-progress', result: failureJump },      { event: 'timeout-progress', result: timeout },      { controlId: 'retry-analysis', result: retry },      { controlId: 'reupload', result: reupload },      { controlId: 'back-home', result: home }    ],    dbReadback: {      failedOrder: client.store.read('diagnosis_orders', orderId),      failedTask: client.store.read('ai_analysis_tasks', 'task-t08-failure'),      retryTask: client.store.read('ai_analysis_tasks', `task-t08-retry-${orderId}`),      allAnalysisTasks: snapshot.ai_analysis_tasks    }  });});test('analysis-failure records owner journey limitation and local-only guard evidence', () => {  const localOnly = assertLocalOnlyEnvironment({    LOCAL_ONLY: 'true',    SCOREMAP_ADAPTER_MODE: 'local-mock'  });  const filesToScan = [    'scoremap-miniapp/pages/analysis/index.js',    'scoremap-miniapp/pages/analysis/analysis-failure.test.js',    'scoremap-miniapp/pages/failure/index.js',    'scoremap-miniapp/services/api-client.js',    'scoremap-miniapp/services/local-fixture-store.js'  ];  const forbiddenRemoteFindings = [];  for (const relativePath of filesToScan) {    const text = fs.readFileSync(path.join(projectRoot, relativePath), 'utf8');    for (const match of scanTextForForbiddenRemoteCalls(text, DEFAULT_FORBIDDEN_PATTERNS)) {      forbiddenRemoteFindings.push({ path: relativePath, match });    }  }  assert.deepEqual(forbiddenRemoteFindings, []);  writeEvidence('analysis-failure-owner-local.json', {    status: 'PASS_WITH_LIMITATION',    command,    requirementIds: ['R03', 'R04', 'R15'],    ownerJourneyEvidence: {      status: 'PASS_WITH_LIMITATION',      scenarios: ['O02', 'O07', 'O08', 'O09', 'O11'],      clickPath: [        '/pages/analysis/index',        'refresh-progress',        '/pages/failure/index',        'retry-analysis',        '/pages/analysis/index',        '/pages/failure/index',        'reupload',        '/pages/index/index'      ],      apiEvidence: 'docs/auto-execute/evidence/frontend-page/analysis-failure-api-db.json',      dbEvidence: 'docs/auto-execute/evidence/frontend-page/analysis-failure-api-db.json',      limitation: 'T08 records deterministic C03/C04 owner actions. Full rendered O01-O12 owner E2E remains assigned to T15.'    },    visualEvidence: {      status: 'PASS_NEEDS_MANUAL_UI_REVIEW',      references: [        'ui-reference-catalog/小程序/AI分析.png',        'ui-reference-catalog/小程序/处理失败.png',        'ui-reference-catalog/小程序/stitch_codex_development_blueprints/ai_1/screen-reference',        'ui-reference-catalog/小程序/stitch_codex_development_blueprints/_3/screen-reference'      ],      expectedVisualCommand: 'npm run visual:scoremap -- analysis failure',      limitation: 'T08 visual runner produces deterministic structural artifacts and metrics. Pixel-perfect screenshot capture remains assigned to T14.'    },    localOnly,    forbiddenRemoteFindings,    secretFindings: []  });});