const path = require('node:path');const { writeEvidenceFile } = require('../../../shared/evidence-paths');const { createMiniappApiClient } = require('../../services/api-client');const { createWrongQuestionPageState } = require('./index');const projectRoot = path.resolve(__dirname, '..', '..', '..');const visualRoot = path.join('frontend-page', 'visual');function runWrongQuestionVisualEvidence(args = process.argv.slice(2)) {  if (!args.includes('v13-wrong-question-detail')) {    throw new Error(`visual:scoremap for T26 requires v13-wrong-question-detail, received: ${args.join(' ')}`);  }  const client = createMiniappApiClient();  client.request('POST', '/api/payments/create', {    orderId: 'order-t26-visual',    paymentType: 'full',    source: 'T26-visual-fixture'  });  client.request('POST', '/api/diagnosis-orders/order-t26-visual/generate-full', {    source: 'T26-visual-fixture'  });  const page = createWrongQuestionPageState(client, { orderId: 'order-t26-visual' });  const state = page.getState();  const metrics = writeScreenVisual('v13-wrong-question-detail', state, {    reference: 'ui-reference-catalog/小程序/stitch_codex_ui_design_kit/_1/screen-reference',    stitchReference: 'ui-reference-catalog/小程序/stitch_codex_ui_design_kit/_1/code.html',    structuralChecks: {      hasV13Reference: state.uiReference.id === 'V13-UI-QUESTION-DETAIL',      hasOriginalQuestion: Boolean(state.question.originalQuestion),      hasStudentAnswer: Boolean(state.question.studentAnswer),      hasCorrectAnswer: Boolean(state.question.correctAnswer),      hasKnowledgePoint: Boolean(state.question.knowledgePoint),      hasDiagnosis: Boolean(state.question.diagnosis),      hasExplanationSummary: Boolean(state.question.explanationSummary),      hasAiTeacherCta: state.aiTutorCta.text === '让 AI 老师讲给孩子听' && state.aiTutorCta.enabled === true,      hasHistoryRow: state.historyRow.text === '本题互动记录',      hasBottomNav: state.controls.some((control) => control.id === 'bottom-share-report')        && state.controls.some((control) => control.id === 'bottom-ai-tutor')        && state.controls.some((control) => control.id === 'bottom-export-pdf')    },    body: renderQuestionBody(state)  });  process.stdout.write(`T26 v1.3 visual evidence written to ${path.relative(projectRoot, path.join(visualRoot, 'v13-wrong-question-detail', 'summary-v13-wrong-question-detail.json'))}\n`);  return [metrics];}function writeScreenVisual(name, state, options) {  writeEvidenceFile(projectRoot, path.join(visualRoot, name, `actual-${name}-structure.svg`), renderShellSvg(state.title, options.body));  writeEvidenceFile(projectRoot, path.join(visualRoot, name, `diff-${name}-manual-review.svg`), renderDiffSvg(name));  const metrics = {    status: 'PASS_NEEDS_MANUAL_UI_REVIEW',    command: 'npm run visual:scoremap -- v13-wrong-question-detail',    route: state.route,    reference: options.reference,    stitchReference: options.stitchReference,    actual: `docs/auto-execute/evidence/frontend-page/visual/${name}/actual-${name}-structure.svg`,    diff: `docs/auto-execute/evidence/frontend-page/visual/${name}/diff-${name}-manual-review.svg`,    viewport: { width: 390, height: 844 },    structuralChecks: options.structuralChecks,    pixelDiff: {      status: 'MANUAL_REVIEW_REQUIRED',      ratio: null,      reason: 'This T26 runner produces deterministic structural visual evidence only; T30 owns full raster screenshot and pixel comparison.'    }  };  writeEvidenceFile(projectRoot, path.join(visualRoot, name, `metrics-${name}.json`), `${JSON.stringify(metrics, null, 2)}\n`);  writeEvidenceFile(projectRoot, path.join(visualRoot, name, `summary-${name}.json`), `${JSON.stringify({ status: metrics.status, metrics }, null, 2)}\n`);  return metrics;}function renderQuestionBody(state) {  const q = state.question;  return `  <text x="28" y="92" font-size="13" fill="#474554">错题 ${q.index} / 8</text>  <rect x="18" y="116" width="354" height="580" rx="16" fill="#ffffff" stroke="#c9c4d6"/>  <rect x="42" y="142" width="58" height="28" rx="8" fill="#7360e5"/>  <text x="54" y="161" font-size="11" font-weight="700" fill="#ffffff">错题 ${q.index}</text>  <rect x="110" y="142" width="156" height="28" rx="8" fill="#f1ecf8"/>  <text x="122" y="161" font-size="10" font-weight="700" fill="#5a45cb">${escapeXml(q.knowledgePoint)}</text>  <rect x="286" y="142" width="58" height="28" rx="8" fill="#ffdad6"/>  <text x="298" y="161" font-size="10" font-weight="700" fill="#93000a">${escapeXml(q.masteryStatus)}</text>  ${renderSection(42, 206, '原题', q.originalQuestion)}  <rect x="42" y="276" width="306" height="70" rx="12" fill="#f9f9f9" stroke="#c9c4d6"/>  <text x="50" y="302" font-size="15" font-weight="700" fill="#1c1b23">学生作答</text>  <text x="50" y="326" font-size="12" fill="#474554">${escapeXml(q.studentAnswer)}</text>  <text x="42" y="388" font-size="15" font-weight="700" fill="#1c1b23">正确答案</text>  <text x="42" y="414" font-size="15" font-weight="700" fill="#5a45cb">${escapeXml(q.correctAnswer)}</text>  <text x="198" y="388" font-size="15" font-weight="700" fill="#1c1b23">知识点</text>  <text x="198" y="414" font-size="12" fill="#474554">${escapeXml(q.knowledgePoint)}</text>  <rect x="42" y="450" width="306" height="82" rx="16" fill="#f7f1fd"/>  <text x="58" y="478" font-size="15" font-weight="700" fill="#1c1b23">错因诊断</text>  <text x="58" y="504" font-size="12" fill="#474554">${escapeXml(q.diagnosis)}</text>  <text x="42" y="574" font-size="15" font-weight="700" fill="#1c1b23">讲解摘要</text>  <text x="42" y="602" font-size="12" fill="#474554">${escapeXml(q.explanationSummary)}</text>  <circle cx="318" cy="604" r="32" fill="#e5deff"/>  <text x="302" y="612" font-size="18" font-weight="800" fill="#442bb5">AI</text>  <rect x="42" y="646" width="306" height="42" rx="16" fill="#5a45cb"/>  <text x="104" y="673" font-size="14" font-weight="700" fill="#ffffff">${escapeXml(state.aiTutorCta.text)}</text>  <rect x="18" y="716" width="354" height="64" rx="16" fill="#ebe6f2"/>  <text x="42" y="746" font-size="15" font-weight="700" fill="#1c1b23">${escapeXml(state.historyRow.text)} (${state.historyRow.count})</text>  <text x="42" y="768" font-size="11" fill="#474554">${escapeXml(state.historyRow.latest)}</text>  <rect x="18" y="804" width="108" height="34" rx="14" fill="#ffffff" stroke="#c9c4d6"/>  <text x="50" y="826" font-size="11" font-weight="700" fill="#474554">分享报告</text>  <rect x="141" y="804" width="108" height="34" rx="14" fill="#7360e5"/>  <text x="176" y="826" font-size="11" font-weight="700" fill="#ffffff">AI 提分</text>  <rect x="264" y="804" width="108" height="34" rx="14" fill="#ffffff" stroke="#c9c4d6"/>  <text x="296" y="826" font-size="11" font-weight="700" fill="#474554">导出 PDF</text>`;}function renderSection(x, y, title, body) {  return `<rect x="${x}" y="${y - 20}" width="4" height="24" rx="2" fill="#5a45cb"/>  <text x="${x + 12}" y="${y}" font-size="15" font-weight="700" fill="#1c1b23">${escapeXml(title)}</text>  <text x="${x}" y="${y + 30}" font-size="12" fill="#474554">${escapeXml(body)}</text>`;}function renderShellSvg(title, body) {  const content = body.trim();  return `<svg xmlns="urn:scoremap:local-svg" width="390" height="844" viewBox="0 0 390 844">  <rect width="390" height="844" fill="#fdf8ff"/>  <text x="54" y="56" font-size="20" font-weight="700" fill="#5a45cb">${escapeXml(title)}</text>  ${content}</svg>`;}function renderDiffSvg(name) {  return `<svg xmlns="urn:scoremap:local-svg" width="390" height="844" viewBox="0 0 390 844">  <rect width="390" height="844" fill="#ffffff"/>  <text x="28" y="80" font-size="18" font-weight="700" fill="#111827">Manual UI review required</text>  <text x="28" y="116" font-size="13" fill="#4b5563">T26 generated structural ${escapeXml(name)} visual evidence. Pixel diff is owned by T30.</text></svg>`;}function escapeXml(value) {  return String(value)    .replace(/&/g, '&amp;')    .replace(/</g, '&lt;')    .replace(/>/g, '&gt;')    .replace(/"/g, '&quot;');}if (require.main === module) {  runWrongQuestionVisualEvidence();}module.exports = { runWrongQuestionVisualEvidence };