const fs = require('node:fs');const path = require('node:path');const { writeEvidenceFile } = require('../../../shared/evidence-paths');const { createHomeUploadPageState } = require('./index');const projectRoot = path.resolve(__dirname, '..', '..', '..');const visualRoot = path.join('frontend-page', 'visual', 'home');function runHomeVisualEvidence(args = process.argv.slice(2)) {  if (args.includes('analysis') || args.includes('failure')) {    const { runAnalysisFailureVisualEvidence } = require('../analysis/visual-analysis-failure');    return runAnalysisFailureVisualEvidence(args);  }  if (args.includes('preview') || args.includes('basic-pay')) {    const { runPreviewBasicPayVisualEvidence } = require('../preview/visual-preview-basic-pay');    return runPreviewBasicPayVisualEvidence(args);  }  if (args.includes('basic-result') || args.includes('full-unlock')) {    const { runBasicResultFullUnlockVisualEvidence } = require('../basic-result/visual-basic-result-full-unlock');    return runBasicResultFullUnlockVisualEvidence(args);  }  if (args.includes('full-report-entry') || args.includes('full-report')) {    const { runFullReportPdfVisualEvidence } = require('../full-report/visual-full-report-pdf');    return runFullReportPdfVisualEvidence(args);  }  if (args.includes('my') || args.includes('reports')) {    const { runMyReportsVisualEvidence } = require('../my/visual-my-reports');    return runMyReportsVisualEvidence(args);  }  if (args.length > 0 && !args.includes('home')) {    throw new Error(`visual:scoremap for T07 only supports home, received: ${args.join(' ')}`);  }  const page = createHomeUploadPageState();  const state = page.getState();  const referencePath = 'ui-reference-catalog/小程序/首页.png';  const stitchReferencePath = 'ui-reference-catalog/小程序/stitch_codex_development_blueprints/ai_2/screen-reference';  const actualPath = writeEvidenceFile(projectRoot, path.join(visualRoot, 'actual-home-structure.svg'), renderHomeSvg(state));  const diffPath = writeEvidenceFile(projectRoot, path.join(visualRoot, 'diff-home-manual-review.svg'), renderDiffSvg());  const metrics = {    status: 'PASS_NEEDS_MANUAL_UI_REVIEW',    command: 'npm run visual:scoremap -- home',    route: '/pages/index/index',    reference: referencePath,    stitchReference: stitchReferencePath,    actual: 'docs/auto-execute/evidence/frontend-page/visual/home/actual-home-structure.svg',    diff: 'docs/auto-execute/evidence/frontend-page/visual/home/diff-home-manual-review.svg',    viewport: { width: 390, height: 844 },    structuralChecks: {      hasLogoTitle: true,      hasUploadCard: true,      hasPrivacyAuthorization: true,      hasSampleEntry: true,      hasMyReportsEntry: true,      hasRecentReports: true,      hasBottomTabs: true    },    pixelDiff: {      status: 'MANUAL_REVIEW_REQUIRED',      ratio: null,      reason: 'This T07 runner produces deterministic structural visual evidence only; T14 owns full screenshot and pixelmatch harness.'    }  };  const metricsPath = writeEvidenceFile(projectRoot, path.join(visualRoot, 'metrics-home.json'), `${JSON.stringify(metrics, null, 2)}\n`);  const summaryPath = writeEvidenceFile(projectRoot, path.join(visualRoot, 'summary-home.json'), `${JSON.stringify({ status: metrics.status, metrics }, null, 2)}\n`);  process.stdout.write(`T07 home visual evidence written to ${path.relative(projectRoot, summaryPath)}\n`);  return metrics;}function renderHomeSvg(state) {  const controls = state.controls.map((control, index) => {    const y = 398 + index * 42;    return `<rect x="48" y="${y}" width="294" height="32" rx="8" fill="#f5f7fb" stroke="#d5dae6"/><text x="64" y="${y + 21}" font-size="14" fill="#1f2937">${escapeXml(control.text)}</text>`;  }).join('');  return `<svg xmlns="urn:scoremap:local-svg" width="390" height="844" viewBox="0 0 390 844">  <rect width="390" height="844" fill="#f7f4ef"/>  <text x="28" y="70" font-size="26" font-weight="700" fill="#1f2937">${escapeXml(state.title)}</text>  <text x="28" y="100" font-size="13" fill="#5f6b7a">上传试卷，先看免费初判</text>  <rect x="28" y="132" width="334" height="214" rx="18" fill="#ffffff" stroke="#e4e0d8"/>  <text x="52" y="178" font-size="22" font-weight="700" fill="#111827">${escapeXml(state.uploadCard.title)}</text>  <text x="52" y="210" font-size="13" fill="#6b7280">${escapeXml(state.uploadCard.description)}</text>  <rect x="52" y="246" width="286" height="58" rx="16" fill="#16a34a"/>  <text x="131" y="283" font-size="18" font-weight="700" fill="#ffffff">上传资料</text>  <text x="52" y="334" font-size="12" fill="#6b7280">上传前展示资料授权弹窗，本地 mock 保存授权状态</text>  <text x="28" y="386" font-size="16" font-weight="700" fill="#111827">可点击控件</text>  ${controls}  <rect x="0" y="770" width="390" height="74" fill="#ffffff" stroke="#e5e7eb"/>  <text x="91" y="812" font-size="13" fill="#16a34a">首页</text>  <text x="284" y="812" font-size="13" fill="#6b7280">我的</text></svg>`;}function renderDiffSvg() {  return `<svg xmlns="urn:scoremap:local-svg" width="390" height="844" viewBox="0 0 390 844">  <rect width="390" height="844" fill="#ffffff"/>  <text x="28" y="80" font-size="18" font-weight="700" fill="#111827">Manual UI review required</text>  <text x="28" y="116" font-size="13" fill="#4b5563">T07 generated structural visual evidence. Pixel diff is owned by T14.</text></svg>`;}function escapeXml(value) {  return String(value)    .replace(/&/g, '&amp;')    .replace(/</g, '&lt;')    .replace(/>/g, '&gt;')    .replace(/"/g, '&quot;');}if (require.main === module) {  runHomeVisualEvidence();}module.exports = { runHomeVisualEvidence };