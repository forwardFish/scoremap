const fs = require('node:fs');const path = require('node:path');const { writeEvidenceFile } = require('../../../shared/evidence-paths');const { createMiniappApiClient } = require('../../services/api-client');const { createReportsPageState } = require('../reports');const { createMyPageState } = require('./index');const projectRoot = path.resolve(__dirname, '..', '..', '..');const visualRoot = path.join('frontend-page', 'visual');function runMyReportsVisualEvidence(args = process.argv.slice(2)) {  if (!args.includes('my') || !args.includes('reports')) {    throw new Error(`visual:scoremap for T12 requires my reports, received: ${args.join(' ')}`);  }  const client = createMiniappApiClient();  const myPage = createMyPageState(client);  const myState = myPage.getState();  const reportsPage = createReportsPageState(client);  const reportsState = reportsPage.getState();  const myMetrics = writeScreenVisual('my', myState, {    reference: 'ui-reference-catalog/小程序/我的.png',    stitchReference: 'ui-reference-catalog/小程序/stitch_codex_development_blueprints/_1/screen-reference',    structuralChecks: {      hasProfile: Boolean(myState.profile.userId),      hasRightsPlaceholder: myState.rightsCard.title === '我的权益' && /no subscription/i.test(myState.rightsCard.proTreatment),      hasStats: myState.stats.length === 3,      hasReportsEntry: myState.quickEntries.some((entry) => entry.id === 'open-reports'),      hasOrdersEntry: myState.quickEntries.some((entry) => entry.id === 'open-orders'),      hasPurchasesEntry: myState.quickEntries.some((entry) => entry.id === 'open-purchases'),      hasFeedbackEntry: myState.quickEntries.some((entry) => entry.id === 'open-feedback'),      hasNewAnalysisButton: myState.controls.some((control) => control.id === 'new-analysis'),      hasBottomTab: myState.bottomTabs.some((tab) => tab.id === 'my' && tab.active)    },    body: renderMyBody(myState)  });  const reportsMetrics = writeScreenVisual('reports', reportsState, {    reference: 'PRD C12 我的报告列表页',    stitchReference: null,    structuralChecks: {      hasStatusTabs: reportsState.filterTabs.length >= 4,      hasReportCards: reportsState.cards.length >= 2,      hasAnalyzingCard: reportsState.cards.some((card) => card.targetRoute === '/pages/analysis/index'),      hasFullReportCard: reportsState.cards.some((card) => card.targetRoute === '/pages/full-report/index'),      hasPaymentStatus: reportsState.cards.some((card) => card.paymentStatus === 'paid'),      noGenericEmptyDashboard: reportsState.emptyState.visible === false    },    body: renderReportsBody(reportsState)  });  process.stdout.write(`T12 visual evidence written to ${path.relative(projectRoot, path.join(visualRoot, 'my', 'summary-my.json'))} and ${path.relative(projectRoot, path.join(visualRoot, 'reports', 'summary-reports.json'))}\n`);  return [myMetrics, reportsMetrics];}function writeScreenVisual(name, state, options) {  writeEvidenceFile(projectRoot, path.join(visualRoot, name, `actual-${name}-structure.svg`), renderShellSvg(state.title, options.body));  writeEvidenceFile(projectRoot, path.join(visualRoot, name, `diff-${name}-manual-review.svg`), renderDiffSvg(name));  const metrics = {    status: 'PASS_NEEDS_MANUAL_UI_REVIEW',    command: 'npm run visual:scoremap -- my reports',    route: state.route,    reference: options.reference,    stitchReference: options.stitchReference,    actual: `docs/auto-execute/evidence/frontend-page/visual/${name}/actual-${name}-structure.svg`,    diff: `docs/auto-execute/evidence/frontend-page/visual/${name}/diff-${name}-manual-review.svg`,    viewport: { width: 390, height: 844 },    structuralChecks: options.structuralChecks,    pixelDiff: {      status: 'MANUAL_REVIEW_REQUIRED',      ratio: null,      reason: 'This T12 runner produces deterministic structural visual evidence only; T14 owns full screenshot and pixelmatch harness.'    }  };  writeEvidenceFile(projectRoot, path.join(visualRoot, name, `metrics-${name}.json`), `${JSON.stringify(metrics, null, 2)}\n`);  writeEvidenceFile(projectRoot, path.join(visualRoot, name, `summary-${name}.json`), `${JSON.stringify({ status: metrics.status, metrics }, null, 2)}\n`);  return metrics;}function renderMyBody(state) {  const stats = state.stats.map((item, index) => {    const x = 36 + index * 108;    return `<rect x="${x}" y="232" width="96" height="62" rx="14" fill="#ffffff" stroke="#e5e7eb"/><text x="${x + 16}" y="258" font-size="18" font-weight="700" fill="#111827">${item.value}</text><text x="${x + 12}" y="280" font-size="11" fill="#6b7280">${escapeXml(item.label)}</text>`;  }).join('');  const entries = state.quickEntries.map((entry, index) => {    const y = 336 + index * 58;    return `<rect x="28" y="${y}" width="334" height="46" rx="14" fill="#ffffff" stroke="#e5e7eb"/><text x="52" y="${y + 29}" font-size="14" font-weight="700" fill="#111827">${escapeXml(entry.text)}</text><text x="316" y="${y + 29}" font-size="14" fill="#9ca3af">›</text>`;  }).join('');  return `  <rect x="28" y="104" width="334" height="88" rx="20" fill="#ffffff" stroke="#e5e7eb"/>  <circle cx="72" cy="148" r="24" fill="#dbeafe"/>  <text x="55" y="154" font-size="14" font-weight="700" fill="#1d4ed8">${escapeXml(state.profile.avatarText)}</text>  <text x="112" y="136" font-size="16" font-weight="700" fill="#111827">${escapeXml(state.profile.nickname)}</text>  <text x="112" y="162" font-size="11" fill="#6b7280">${escapeXml(state.profile.userId)}</text>  <rect x="28" y="204" width="334" height="84" rx="18" fill="#fff7ed" stroke="#fed7aa"/>  <text x="48" y="226" font-size="15" font-weight="700" fill="#111827">${escapeXml(state.rightsCard.title)}</text>  ${stats}  ${entries}  <rect x="28" y="612" width="334" height="56" rx="22" fill="#2563eb"/>  <text x="130" y="647" font-size="16" font-weight="700" fill="#ffffff">继续新建分析</text>  <rect x="0" y="770" width="390" height="74" fill="#ffffff" stroke="#e5e7eb"/>  <text x="91" y="812" font-size="13" fill="#6b7280">首页</text>  <text x="284" y="812" font-size="13" fill="#2563eb">我的</text>`;}function renderReportsBody(state) {  const tabs = state.filterTabs.map((tab, index) => {    const x = 28 + index * 84;    return `<rect x="${x}" y="110" width="74" height="30" rx="14" fill="${tab.active ? '#111827' : '#ffffff'}" stroke="#d1d5db"/><text x="${x + 16}" y="130" font-size="11" fill="${tab.active ? '#ffffff' : '#374151'}">${escapeXml(tab.text)}</text>`;  }).join('');  const cards = state.cards.map((card, index) => {    const y = 172 + index * 126;    return `<rect x="28" y="${y}" width="334" height="104" rx="16" fill="#ffffff" stroke="#e5e7eb"/>  <text x="48" y="${y + 28}" font-size="15" font-weight="700" fill="#111827">${escapeXml(card.title)}</text>  <text x="48" y="${y + 52}" font-size="11" fill="#6b7280">${escapeXml(card.grade)} · ${escapeXml(card.subject)} · ${escapeXml(card.examType)}</text>  <text x="48" y="${y + 78}" font-size="12" fill="#2563eb">${escapeXml(card.statusText)} / ${escapeXml(card.paymentStatus)}</text>  <text x="270" y="${y + 78}" font-size="12" font-weight="700" fill="#111827">${escapeXml(card.actionText)}</text>`;  }).join('');  return `  ${tabs}  ${cards}  <rect x="28" y="692" width="334" height="46" rx="18" fill="#f8fafc" stroke="#d1d5db"/>  <text x="124" y="722" font-size="13" fill="#374151">GET /api/my/reports</text>`;}function renderShellSvg(title, body) {  const content = body.trim();  return `<svg xmlns="urn:scoremap:local-svg" width="390" height="844" viewBox="0 0 390 844">  <rect width="390" height="844" fill="#f6f7fb"/>  <text x="28" y="72" font-size="24" font-weight="700" fill="#111827">${escapeXml(title)}</text>  ${content}</svg>`;}function renderDiffSvg(name) {  return `<svg xmlns="urn:scoremap:local-svg" width="390" height="844" viewBox="0 0 390 844">  <rect width="390" height="844" fill="#ffffff"/>  <text x="28" y="80" font-size="18" font-weight="700" fill="#111827">Manual UI review required</text>  <text x="28" y="116" font-size="13" fill="#4b5563">T12 generated structural ${escapeXml(name)} visual evidence. Pixel diff is owned by T14.</text></svg>`;}function escapeXml(value) {  return String(value)    .replace(/&/g, '&amp;')    .replace(/</g, '&lt;')    .replace(/>/g, '&gt;')    .replace(/"/g, '&quot;');}if (require.main === module) {  runMyReportsVisualEvidence();}module.exports = { runMyReportsVisualEvidence };